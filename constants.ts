import { CleaningFrequency, CleaningType, TaskStatus } from './types';

export const HOURLY_RATE_DKK = 349;

export const CLEANING_TYPE_OPTIONS = Object.values(CleaningType);
export const CLEANING_FREQUENCY_OPTIONS = Object.values(CleaningFrequency);
export const TASK_STATUS_OPTIONS = Object.values(TaskStatus);

export const APP_NAME = "Rendetalje";
export const DENMARK_LOCALE = 'da-DK';